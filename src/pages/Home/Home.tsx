import { Button } from "@/components/ui/button";

const Home = () => {
  return (
    <div>
      <p>this is home</p>
      <Button className="cursor-pointer ">Click me</Button>
    </div>
  );
};

export default Home;
